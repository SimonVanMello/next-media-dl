import {
  Html,
  Head,
  Main,
  NextScript,
} from 'next/document';

const Document = () => (
  <Html lang="en" className="min-h-screen">
    <Head />
    <body className="min-h-screen">
      <Main />
      <NextScript />
    </body>
  </Html>
);

export default Document;
